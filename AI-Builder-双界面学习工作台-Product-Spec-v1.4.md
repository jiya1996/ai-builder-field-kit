# AI Builder Field Kit · 双界面学习工作台 Product Spec v1.4

> 状态：根据 2026-07-30 讨论更新
> 替代：v1.3 的“四个一级页面”方案

## 1. 产品界面

产品只保留两个一级界面：

| 界面 | 主要职责 |
|---|---|
| Landing Page | 解释产品价值和三部分交付 |
| AI Builder 学习工作台 | 在同一上下文中完成 S00–S10 理论、实战、验收与 Agent 辅导 |

理论知识、课程实战和辅导 Agent 不再是三个割裂的产品界面，而是学习工作台中的三项协同能力。

## 2. Landing Page

首页只包含：

1. 核心结果：学会软件如何运转、AI 如何协作、产品如何从想法走到上线。
2. 用户最终获得的变化：不只拥有一个作品，还能掌握下一次独立完成产品的方法。
3. 三部分共同交付：理论知识、课程实战、辅导 Agent。
4. 进入 S00–S10 学习工作台的入口。

首页不展开课程目录、工具大表、详细正文和旧“五关/45 节/7 章”数字。

## 3. 学习工作台

### 3.1 桌面结构

```text
顶部：当前位置、总体进度、Agent 开关

左侧：S00–S10 唯一主线
中间：当前阶段的理论 / 实战 / 验收
右侧：读取当前阶段上下文的辅导 Agent
```

移动端：

- S00–S10 变成顶部横向阶段导航。
- 理论、实战、验收保留为当前阶段页签。
- Agent 从右侧栏变成可打开的抽屉。

### 3.2 唯一阶段循环

```text
理解概念 → 完成实战 → 提交证据 → Agent 判断下一步
    ↑                                  ↓
    └──────────── 需要时补课或修任务 ───┘
```

每个阶段固定包含：

- 输入什么。
- 输出什么。
- 学会什么。
- 核心问题与关键概念。
- 三组以上机制性知识讲解。
- 学完必须掌握。
- 理解检查。
- 默认工具与选择理由。
- 具体实战步骤。
- 完成证据。
- Boss 验收。
- 一手资料。
- Agent 当前辅导目标、诊断和下一步。

## 4. URL 与返回

学习体验使用同一个工作台，但必须保留可分享状态：

```text
/learn/s00?view=theory
/learn/s03?view=practice
/learn/s06?view=evidence
```

要求：

- 切换阶段或视图时地址同步变化。
- 浏览器返回时恢复上一状态。
- 刷新后保持当前阶段。
- 旧 `/knowledge`、`/action`、`/coach` 地址继续可用，但显示同一个学习工作台。
- 旧 `/learn/g*/课节/` 地址继续可用，并在映射后的 S 阶段工作台中展开对应正文。
- `/learn?stage=s00&view=theory` 查询参数形式继续兼容，但新的站内链接使用阶段静态路径。
- 任意学习状态都能返回 Landing Page。

### 4.1 阶段作品必须先于课程目录出现

用户进入任一阶段时，必须立刻看到“本阶段要做的东西”，不能等到切换至实战页后才知道交付物。理论、操作步骤与验收证据均围绕这一个具体作品展开。

S00 是第一阶段，对外名称保持“第一次成功”。它的唯一作品是：

> 用 AI 做一个属于自己的个人介绍网页，并让任何人都能通过链接打开。

这里的 AI 是开发搭档，不是第一版成品里的功能：用户先与 AI 澄清受众、内容、结构、边界和验收条件，再让 Coding Agent 实现。网页至少包含姓名、身份介绍、个人经历、作品或兴趣和联系方式；第一版不做登录、数据库、支付或 AI 问答。完成证据包括公开网址、GitHub 仓库、他人访问结果以及一次故障—定位—修复记录。

## 5. Agent 边界

当前交付为“阶段辅导 Beta”，即基于课程规则的结构化自检：

- 知道当前 S 阶段。
- 知道本阶段理论、任务和验收。
- 能解释概念、检查证据并建议下一步。
- 能生成本阶段 Coding Agent 任务提示词。

当前不声称：

- 自动读取用户代码。
- 声称已经连接真实模型或提供开放式个性化对话。
- 自动执行项目。
- 跨设备长期记忆。
- 替用户判断任务已经完成。

## 6. 验收标准

- 首页 30 秒内能让用户说出产品承诺与三部分交付。
- 首页不再出现旧课程数字和重复目录。
- S00–S10 是全站唯一主线。
- 任一阶段都能在同一界面切换理解、实战和验收。
- 每条证据与 Boss 验收都可单独勾选，勾选状态保存在本机。
- 只有全部证据与 Boss 验收均已确认，才允许标记阶段通关；撤销任一验收项时同步撤销通关状态。
- 切换阶段后 Agent 上下文同步改变。
- 实战步骤和阶段完成状态可在本机保存。
- 桌面端三区域协同，移动端无横向溢出。
- 旧公开链接不失效。
